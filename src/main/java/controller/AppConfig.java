package controller;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // ⚠️ NU MAI AI NEVOIE DE NIMIC AICI.

    // Spring Boot detectează automat:
    // 1. Repository-urile (pentru că extind JpaRepository)
    // 2. Service-urile (pentru că au adnotarea @Service)
    // 3. Controller-ele (pentru că au adnotarea @Controller)
}